apiVersion: v1
kind: Service
metadata:
  name: {{ include "accountchart.fullname" . }}
  labels:
    {{- include "accountchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accountchart.selectorLabels" . | nindent 4 }}
